import React from 'react';
import Layout from '@theme/Layout';
import Main from '../components/MainPage';
const Home = () => {
  return (
    <Layout>
      <Main />
    </Layout>
  );
};

export default Home;
